---
phase: 09-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - feature-flags/src/api/v1/environments.py
  - feature-flags/src/api/v1/flags.py
  - feature-flags/src/main.py
  - feature-flags/src/infra/metrics.py
  - feature-flags/sdk/feature_flags_sdk/models.py
  - feature-flags/sdk/feature_flags_sdk/__init__.py
  - feature-flags/docker-compose.yml
autonomous: true
requirements: [REQ-09]
must_haves:
  truths:
    - src/api/v1/environments.py existe com endpoints POST enable e POST disable
    - enable/disable removidos de flags.py (sem duplicata de rotas)
    - environments_router registrado em main.py
    - webhook_dispatch_duration_seconds Histogram existe em metrics.py
    - sdk/feature_flags_sdk/models.py existe com EvaluationResult e FlagValue
    - EvaluationResult e FlagValue exportados em sdk/__init__.py
    - docker-compose.yml na raiz de feature-flags/ com postgres, redis, api
  artifacts:
    - feature-flags/src/api/v1/environments.py
    - feature-flags/sdk/feature_flags_sdk/models.py
    - feature-flags/docker-compose.yml
  key_links:
    - environments.py importa helpers (_get_flag_or_404, _insert_audit, _publish_invalidation) de flags.py
    - main.py importa environments_router e inclui com prefix="/api/v1"
    - sdk/__init__.py exporta EvaluationResult e FlagValue de .models
---

<objective>
Fechar 4 lacunas identificadas vs prompt.md após auditoria de Phase 8.

Purpose: O prompt.md exige environments.py, sdk/models.py, docker-compose.yml e 2 histogramas de métricas que estavam ausentes na implementação das Phases anteriores.
Output: 3 novos arquivos criados, 4 arquivos modificados, promovendo conformidade completa com a spec.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@feature-flags/src/api/v1/flags.py
@feature-flags/src/main.py
@feature-flags/src/infra/metrics.py
@feature-flags/sdk/feature_flags_sdk/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/api/v1/environments.py com endpoints enable/disable</name>
  <files>feature-flags/src/api/v1/environments.py</files>
  <read_first>feature-flags/src/api/v1/flags.py</read_first>
  <action>
Criar novo arquivo `src/api/v1/environments.py` com router de prefix="/flags" e tag "environments".
Importar helpers privados de flags.py: _get_flag_or_404, _insert_audit, _publish_invalidation.
Implementar dois endpoints:
  POST /{flag_key}/environments/{env}/enable → enable_flag_in_environment
  POST /{flag_key}/environments/{env}/disable → disable_flag_in_environment
Lógica idêntica às funções que estão em flags.py (envs[env] = {**envs[env], "enabled": True/False}).
response_model=FlagResponse, dependencies=[Depends(require_role("admin", "editor"))].
  </action>
  <verify>python -c "from src.api.v1.environments import router; print(router.prefix)"</verify>
  <acceptance_criteria>
    - environments.py contém "router = APIRouter(prefix=\"/flags\""
    - environments.py contém "async def enable_flag_in_environment"
    - environments.py contém "async def disable_flag_in_environment"
  </acceptance_criteria>
  <done>environments.py existe com os dois endpoints POST enable e POST disable</done>
</task>

<task type="auto">
  <name>Task 2: Remover enable/disable de flags.py e registrar environments_router em main.py</name>
  <files>feature-flags/src/api/v1/flags.py, feature-flags/src/main.py</files>
  <read_first>feature-flags/src/api/v1/flags.py, feature-flags/src/main.py</read_first>
  <action>
Em flags.py: remover os dois @router.post para "/{flag_key}/environments/{env}/enable" e
"/{flag_key}/environments/{env}/disable" e suas funções de handler. Manter o comentário de seção
se existir, mas removê-lo também se ficar vazio.
Em main.py: na seção de Routers (após registrar webhooks_router), adicionar:
  from src.api.v1.environments import router as environments_router
  app.include_router(environments_router, prefix="/api/v1")
  </action>
  <verify>grep -n "environments" feature-flags/src/main.py</verify>
  <acceptance_criteria>
    - flags.py NÃO contém "/{flag_key}/environments/{env}/enable"
    - main.py contém "from src.api.v1.environments import router as environments_router"
    - main.py contém "app.include_router(environments_router"
  </acceptance_criteria>
  <done>Rotas deduplicadas: enable/disable apenas em environments.py, router registrado em main.py</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar webhook_dispatch_duration_seconds em metrics.py</name>
  <files>feature-flags/src/infra/metrics.py</files>
  <read_first>feature-flags/src/infra/metrics.py</read_first>
  <action>
Na seção "── Webhooks ──", após `webhook_dispatch_total`, adicionar:

webhook_dispatch_duration_seconds = Histogram(
    "webhook_dispatch_duration_seconds",
    "Latência do disparo de webhooks externos (segundos)",
    buckets=(0.1, 0.5, 1.0, 2.0, 5.0, 10.0, 30.0),
)

Nota: pubsub_lag_seconds JÁ existe no arquivo (não adicionar novamente).
  </action>
  <verify>grep "webhook_dispatch_duration_seconds" feature-flags/src/infra/metrics.py</verify>
  <acceptance_criteria>
    - metrics.py contém "webhook_dispatch_duration_seconds = Histogram("
    - metrics.py contém "Latência do disparo de webhooks externos"
  </acceptance_criteria>
  <done>webhook_dispatch_duration_seconds Histogram adicionado a metrics.py</done>
</task>

<task type="auto">
  <name>Task 4: Criar sdk/feature_flags_sdk/models.py</name>
  <files>feature-flags/sdk/feature_flags_sdk/models.py</files>
  <read_first>feature-flags/sdk/feature_flags_sdk/__init__.py, feature-flags/sdk/feature_flags_sdk/client.py</read_first>
  <action>
Criar `sdk/feature_flags_sdk/models.py` com dataclasses leves (sem Pydantic — SDK não deve depender
de Pydantic). Conteúdo:
  - EvaluationResult: flag_key: str, value: Any, reason: str, environment: str, flag_version: int = 0
  - FlagValue: enabled: bool, variant: Any = None
Usar @dataclass do stdlib + from __future__ import annotations.
  </action>
  <verify>python -c "from feature_flags_sdk.models import EvaluationResult, FlagValue; print('ok')"</verify>
  <acceptance_criteria>
    - models.py contém "@dataclass" e "class EvaluationResult"
    - models.py contém "@dataclass" e "class FlagValue"
  </acceptance_criteria>
  <done>sdk/models.py criado com EvaluationResult e FlagValue como dataclasses</done>
</task>

<task type="auto">
  <name>Task 5: Exportar modelos do SDK em __init__.py</name>
  <files>feature-flags/sdk/feature_flags_sdk/__init__.py</files>
  <read_first>feature-flags/sdk/feature_flags_sdk/__init__.py, feature-flags/sdk/feature_flags_sdk/models.py</read_first>
  <action>
Atualizar `sdk/feature_flags_sdk/__init__.py` para importar e exportar EvaluationResult e FlagValue:
  from .models import EvaluationResult, FlagValue
  __all__ = ["FlagClient", "EvaluationResult", "FlagValue"]
  </action>
  <verify>grep "EvaluationResult" feature-flags/sdk/feature_flags_sdk/__init__.py</verify>
  <acceptance_criteria>
    - __init__.py contém "from .models import EvaluationResult, FlagValue"
    - __all__ inclui "EvaluationResult" e "FlagValue"
  </acceptance_criteria>
  <done>EvaluationResult e FlagValue exportados do SDK</done>
</task>

<task type="auto">
  <name>Task 6: Criar docker-compose.yml</name>
  <files>feature-flags/docker-compose.yml</files>
  <read_first>feature-flags/Dockerfile, feature-flags/src/config.py</read_first>
  <action>
Criar `feature-flags/docker-compose.yml` com 4 serviços:
  - postgres: image postgres:16, porta 5432:5432, env POSTGRES_DB=feature_flags POSTGRES_USER=postgres POSTGRES_PASSWORD=postgres, healthcheck
  - redis: image redis:7-alpine, porta 6379:6379, healthcheck
  - prometheus: image prom/prometheus:latest, porta 9090:9090, volumes ./prometheus.yml:/etc/prometheus/prometheus.yml
  - api: build: ., porta 8000:8000, env DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/feature_flags REDIS_URL=redis://redis:6379/0, depends_on postgres+redis (condition: service_healthy)
Usar versão sintaxe compose moderna (sem "version:" key).
  </action>
  <verify>docker compose -f feature-flags/docker-compose.yml config --quiet 2>&amp;1 | head -5</verify>
  <acceptance_criteria>
    - docker-compose.yml contém "image: postgres:16"
    - docker-compose.yml contém "image: redis:7-alpine"
    - docker-compose.yml contém "build: ."
    - docker-compose.yml contém "service_healthy"
  </acceptance_criteria>
  <done>docker-compose.yml criado com postgres + redis + prometheus + api</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -r "environments_router" feature-flags/src/main.py
- [ ] grep "webhook_dispatch_duration_seconds" feature-flags/src/infra/metrics.py
- [ ] test -f feature-flags/src/api/v1/environments.py
- [ ] test -f feature-flags/sdk/feature_flags_sdk/models.py
- [ ] test -f feature-flags/docker-compose.yml
- [ ] grep -c "environments.*enable" feature-flags/src/api/v1/flags.py — deve retornar 0
</verification>

<success_criteria>

- Todos os 6 tasks completados
- environments.py presente com enable/disable, sem duplicata em flags.py
- webhook_dispatch_duration_seconds Histogram em metrics.py
- sdk/models.py com EvaluationResult + FlagValue exportados
- docker-compose.yml funcional com 4 serviços
</success_criteria>
